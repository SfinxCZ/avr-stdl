
#include "lcd.h"

void lcd_init() {

	LCD_DATA_PULLUP = 0xff;
	LCD_COMMAND_PULLUP = 0xff;

	for (int i = 0; i < 20; i++) {
		LCD_COMMAND_PORT |= 1 << LCD_E;
		_delay_ms(100);
		LCD_COMMAND_PORT &= 0 << LCD_E;
		_delay_ms(100);
	}
}
