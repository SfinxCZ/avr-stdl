
#include "lcd.h"

void lcd_init() {

	LCD_DATA_PULLUP = 0xff;
	LCD_COMMAND_PULLUP = 0xff;

	for (int i = 0; i < 20; i++) {
		LCD_COMMAND_PORT = 0xff;
		_delay_ms(1000);
		LCD_COMMAND_PORT = 0;
		_delay_ms(1000);
	}
}
